import CodeBlock from "@/components/shared/CodeBlock"

---
title: "NestJS 모듈 설계와 레이어 구조"
date: "2025-10-31"
tags: ["NestJS", "Architecture", "Backend"]
summary: "NestJS 모듈/레이어 설계, Guard/Interceptor, Prisma Repository, E2E 테스트와 CI 통합을 단계별로 설명합니다."
---

## 요약

NestJS의 **모듈 구조**, **Guard/Interceptor 설계**,  
**Prisma Repository 패턴**, **E2E 테스트 및 CI 통합**까지를 단계별로 정리합니다.

---

## 핵심 포인트

- 도메인 단위의 모듈화
- Guard / Interceptor를 통한 공통 처리
- Repository 계층과 테스트 전략

---

## 예제 코드

<CodeBlock language="ts">
{`import { Module } from "@nestjs/common";

@Module({
  controllers: [UsersController],
  providers: [UsersService],
  exports: [UsersService],
})
export class UsersModule {}`}
</CodeBlock>

---

## 체크리스트

1. DI(의존성 주입) 경계 명확히 구분하기  
2. 공통 규약(예: BaseService, BaseRepository) 수립하기  
3. E2E 테스트를 CI 파이프라인에 통합하기

---

> ⚙️ **참고:**  
> 이 글은 학습 및 실무 정리를 위해 제작된 더미 콘텐츠입니다.
